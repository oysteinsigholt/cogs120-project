<!doctype html>

<html>
<head>
 {{> head }}
</head>

<body>
	<form method="post">
		<div class="navbar-fixed">
			<nav class="top-nav">
		    <div class="nav-wrapper">
		      <span class="brand-logo">{{#if editing}}<small>Edit {{ course.code }}: {{ course.name }}</small>{{else}}Manage Courses{{/if}}</span>
					<ul class="nav-ul left">
						{{#if editing}}
						<li><a href="/manage"><i class="material-icons">chevron_left</i></a></li>
						{{else}}
						<li><a href="#" data-activates="slide-out" class="button-collapse waves-effect"><i class="material-icons">menu</i></a></li>
						{{/if}}
		      </ul>
		    </div>
		  </nav>
		</div>
		{{> sidenav this }}
	<main>
		{{#if editing}}
			{{> course this}}
		{{else}}
    <div class="row">
      <div class="col s12">
        <ul class="tabs" id="ucsd-quarter">
          <li class="tab col s3 disabled"><a href="#FA17">FA17</a></li>
          <li class="tab col s3"><a class="active" href="#WI18">WI18</a></li>
          <li class="tab col s3 disabled"><a href="#SP18">SP18</a></li>
        </ul>
      </div>
    </div>
			{{#if hasCourses}}
      	{{> courselist this }}
			{{else}}
				<div class="container">
					<center>
						<br />
						<h6>Add some courses to get started</h6>
					</center>
				</div>
			{{/if}}
    {{/if}}
	</main>
		<footer>
			{{#if editing}}
				<button class="btn btn-large waves-effect waves-light" id="section-next" type="submit" name="action" value="next">Save
					<i class="material-icons right">send</i>
				</button>

				<button class="btn btn-large waves-effect waves-light" id="section-custom" type="submit" name="action" style="display: none;" disabled>Add Event
					<i class="material-icons right">event</i>
				</button>
			{{else}}
				<a href="/" class="btn-floating btn-large waves-effect waves-light red"><i class="material-icons">add</i></a>
			{{/if}}
		</footer>
	</form>
	{{> script }}
</body>
</html>
